const { list, x, user, sum } = require('./first');//To import other file code we need to call varibales or function for that we need to write like this and then we can operate with it...

console.log(list);
console.log(x);
console.log(user);

let z = sum(100, 200);
console.log(z);