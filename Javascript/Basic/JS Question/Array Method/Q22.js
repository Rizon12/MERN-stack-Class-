//22. Use `forEach` to log the index and value of each element in an array.
let arr = ['apple', 'ball', 'cat', 'bull', 'Hero'];

arr.forEach((element, index) => {
  console.log(`Index ${index}: ${element}`);
});